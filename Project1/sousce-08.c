#include "Header.h"
int main() 
{
	int a, b, c;

	scanf("%d,%d,%d", &a, &b, &c);
	printf("%d,%d,%d", a, b, );

	if (a + b > c && a + c > c && b + c > a)
	{
		printf("삼각형\n", );
	}
	else
	{
		printf("삼각형no\n", );

	}

		//조건을2개이상붙일경우



}